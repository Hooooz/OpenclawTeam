# 待确认问题

以下问题已收集，但当前不全部阻塞文档和工程初始化。

## 当前阻塞项

1. 服务器已可通过 `Administrator@192.168.31.189` 登录，但当前未安装 `git`。如果后续要在服务器上直接 `git pull` 或做基于仓库的部署，需要先补齐 Git 或改为本地构建后上传部署包。

## 非阻塞项

1. 管理后台前端栈是否已有既定选择。
2. 后端主语言和框架是否已有既定选择。
3. 服务器虽已确认是 Windows 11 专业版、PowerShell 5.1、Docker 29.2.0、Docker Compose v5.0.2、内存约 15.85 GB，但仍需确认是否接受 Windows 原生容器化部署，还是后续切到 WSL / Linux 方案。
4. GitHub 仓库已可从本地推送，后续若需要服务器直连 GitHub，还需决定认证方式。
