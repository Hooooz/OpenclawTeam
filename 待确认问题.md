# 待确认问题

以下问题已收集，但当前不全部阻塞文档和工程初始化。

## 当前阻塞项

1. 当前会话环境访问 `192.168.31.189:22` 返回 `No route to host`，后续若要直接部署或探测服务器环境，需要确认网络连通方式。

## 非阻塞项

1. 管理后台前端栈是否已有既定选择。
2. 后端主语言和框架是否已有既定选择。
3. 服务器当前操作系统、Docker、Compose 是否已准备。
4. GitHub 仓库后续推送使用哪种认证方式。
